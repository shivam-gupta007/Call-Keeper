<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.shivamgupta.callkeeper.history.presentation.CallLogsViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/padding_16dp"
        tools:context=".history.presentation.HistoryFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/callLogsRv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:isVisible="@{!vm.isLoading()}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/layout_call_log_card" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            app:isVisible="@{vm.isLoading()}" />

    </RelativeLayout>
</layout>